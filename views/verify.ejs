<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Account Verification — VibeNest</title>
  <link rel="stylesheet" href="/css/theme.css">
</head>
<body>
  <%- include('partials/header') %>
  <div class="auth-card" style="max-width:520px;margin:28px auto;text-align:center;padding:28px">
    <% if (success) { %>
      <h1 class="success">Verified</h1>
    <% } else { %>
      <h1 class="error">Verification Failed</h1>
    <% } %>
    <p><%= message %></p>
    <div style="margin-top:12px"><a class="btn btn-primary" href="/login">Go to Login</a></div>
  </div>
</body>
</html>
