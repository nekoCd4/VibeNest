# Set these to 'true' to show the respective OAuth buttons
GoauthOn=true
EouathOn=true
# Example environment variables for VibeNest
# Copy this file to `.env` and fill values (do NOT commit secrets to git)
the login doesnt center on screen also i wna the vibenest name and this VibeNest
Login
Register i want the login and register two things remvoed and the vibenest name in the top right so the login section is centered
# App
PORT=3000
NODE_ENV=development
SESSION_SECRET=replace-with-a-secure-random-string
BASE_URL=http://localhost:3000
Convert remaining views to use theme.css (I can scan and patch them).
# Support / legal emails
SUPPORT_EMAIL=kinvilleadam@maltaschools.org
LEGAL_EMAIL=kinvilladam134@outlook.com

## Mailjet (primary — used for all email if set)
MAILJET_API_KEY=
MAILJET_API_SECRET=
MAILJET_SMTP_HOST=in-v3.mailjet.com
MAILJET_SMTP_PORT=587
MAILJET_SMTP_SECURE=false

## SMTP (fallback — used only if Mailjet not set)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false

# Google OAuth (optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback

# Microsoft / Entra ID OAuth (optional)
ENTRA_CLIENT_ID=
ENTRA_CLIENT_SECRET=
ENTRA_CALLBACK_URL=http://localhost:3000/auth/entra/callback

# Helpful if using Nodemailer with Gmail (not recommended for production)
NODEMAILER_USER=
NODEMAILER_PASS=
also this Or create a README section describing how to test these features locally. and this Implement TOTP authenticator app verification (full 2FA authenticator flow), and also edit the menu so it has all of the right buttons for everything in the right place like the settings menu with every right button also make it more modern lookign