<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Account Verified</title>
  <link rel="stylesheet" href="/style.css">
  <style>
    .login-link-button {
      display: inline-block;
      text-align: center;
      padding: 12px;
      margin-top: 20px;
      width: 100%;
      box-sizing: border-box;
      border: none;
      border-radius: 5px;
      background: #0a0;
      color: #fff;
      font-weight: bold;
      cursor: pointer;
      font-size: 1em;
      text-decoration: none;
    }
    .login-link-button:hover {
      background: #0f0;
      color: #000;
    }
    .resend-button {
      display: inline-block;
      text-align: center;
      padding: 12px;
      margin-top: 20px;
      width: 100%;
      box-sizing: border-box;
      border: none;
      border-radius: 5px;
      background: #e90;
      color: #fff;
      font-weight: bold;
      cursor: pointer;
      font-size: 1em;
      text-decoration: none;
    }
    .resend-button:hover {
      background: #ffb84d;
      color: #000;
    }
  </style>
</head>
<body id="login-page">
  <div class="login-container">
    <h1>Account Verification</h1>
    <p style="text-align: center; color: white;">
      <%= message %>
    </p>

    <% if (message === "Your account has been successfully verified. You can now log in.") { %>
      <a href="/login" class="login-link-button">Go to Login</a>
    <% } else if (message === "Invalid or expired verification token.") { %>
      <form action="/resend-verification" method="POST">
        <input type="hidden" name="email" value="<%= email || '' %>">
        <button type="submit" class="resend-button">Resend Verification Email</button>
      </form>
    <% } %>
  </div>
</body>
</html>
