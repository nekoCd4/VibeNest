<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Password Reset</title>
  <style>
    body { font-family: system-ui, sans-serif; background: #f5f7fb; display:flex;align-items:center;justify-content:center;height:100vh;margin:0 }
    .card{background:#fff;padding:28px;border-radius:8px;box-shadow:0 6px 20px rgba(0,0,0,0.08);width:360px}
    input{width:100%;padding:10px;margin-top:8px;margin-bottom:12px;border:1px solid #ddd;border-radius:6px}
    button{width:100%;padding:10px;background:#667eea;color:#fff;border:none;border-radius:6px;cursor:pointer}
    .msg{color:#333;margin-bottom:10px}
  </style>
</head>
<body>
  <div class="card">
    <h2>Reset your password</h2>
    <% if (message) { %>
      <div class="msg"><%= message %></div>
    <% } else { %>
      <div class="msg">Enter the email address for your account. We'll send a link to reset your password.</div>
    <% } %>
    <form method="POST" action="/reset">
      <input type="email" name="email" placeholder="you@example.com" required />
      <button type="submit">Send reset link</button>
    </form>
  </div>
</body>
</html>