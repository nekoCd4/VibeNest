Robust server-side fix: keep using the Supabase-hosted callback but implement a server-side fallback (authorization-code / token exchange) so authentication completes even when fragments are unavailable (works well in proxied previews like Codespaces but requires more code and provider config).

