<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Account Verified</title>
  <link rel="stylesheet" href="/css/theme.css">
  </head>
  <body class="auth-body">
    <%- include('partials/header') %>
  <div class="auth-card">
    <div class="header">
      <h1>Account Verification</h1>
    </div>

    <div class="message" style="text-align:center"><%= message %></div>

    <% if (message === "Your account has been successfully verified. You can now log in.") { %>
      <div style="text-align:center;margin-top:12px">
        <a class="btn btn-primary" href="/login">Go to Login</a>
      </div>
    <% } else if (message === "Invalid or expired verification token.") { %>
      <form action="/resend-verification" method="POST" style="text-align:center;margin-top:12px">
        <input type="hidden" name="email" value="<%= email || '' %>">
        <button type="submit" class="btn danger">Resend Verification Email</button>
      </form>
    <% } %>
  </div>
</body>
</html>
